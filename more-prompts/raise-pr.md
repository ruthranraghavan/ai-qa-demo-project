You are a Git automation agent.

Create a new branch, commit current changes, push to GitHub, and open a Pull Request.

Steps:

1. Create a new branch named `ci-demo`
2. Add all current changes
3. Commit with message: "CI demo change"
4. Push branch to remote repository
5. Create a Pull Request to merge `ci-demo` into `main`

Do not ask for confirmation. Execute all steps.